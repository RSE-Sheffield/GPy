name: GPy
channels:
  - defaults
dependencies:
  # install dependencies
  - python=3.7.4
  - scipy
  - numpy
  - matplotlib
  - pip
  - pip:
    - paramz

  # test dependencies
  - pytest=5.4.1
  - coverage
  - nose